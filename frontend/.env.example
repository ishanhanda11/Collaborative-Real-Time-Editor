# Socket.IO server URL (useful when running frontend and backend on separate ports in local dev)
# Leave unset or empty in production / Docker containers to automatically use window.location.origin
VITE_SERVER_URL=http://localhost:3000
